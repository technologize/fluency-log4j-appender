name: Run Maven Clean Install
on: 
  push:
  pull_request:
  release:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Setup Java JDK
        uses: actions/setup-java@v2.1.0
        with:
          java-version: '8'
          distribution: 'adopt'
          check-latest: true
          gpg-private-key: ${{ secrets.OSSRH_GPG_SECRET_KEY }}
          gpg-passphrase: MAVEN_GPG_PASSPHRASE 

      - name: Maven install and deploy to Github
        run: mvn clean install deploy
        env:
          GITHUB_TOKEN: ${{ github.token }}
          MAVEN_GPG_PASSPHRASE: ${{ secrets.OSSRH_GPG_SECRET_KEY_PASSWORD }}

      # - name: Setup Java JDK For Maven Central Deployment
      #   uses: actions/setup-java@v2.1.0
      #   with:
      #     java-version: '8'
      #     distribution: 'adopt'
      #     check-latest: true
      #     server-id: maven
      #     server-username: MAVEN_USERNAME # optional, default is GITHUB_ACTOR
      #     server-password: MAVEN_CENTRAL_TOKEN # optional, default is GITHUB_TOKEN
      #     gpg-private-key: ${{ secrets.OSSRH_GPG_SECRET_KEY }}
      #     gpg-passphrase: MAVEN_GPG_PASSPHRASE 

      # - name: Maven install and deploy to central maven
      #   run: mvn clean install deploy
      #   env:
      #     MAVEN_USERNAME: ${{ secrets.OSSRH_USERNAME }}
      #     MAVEN_CENTRAL_TOKEN: ${{ secrets.OSSRH_TOKEN }}
      #     MAVEN_GPG_PASSPHRASE: ${{ secrets.OSSRH_GPG_SECRET_KEY_PASSWORD }}